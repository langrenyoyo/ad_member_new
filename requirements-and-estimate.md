# 广告会员运营后台整站需求规格说明书

文档版本：V2.0
文档状态：开发基线
编制日期：2026-08-28
目标对标站点：[https://ad.leadink.cn/](https://ad.leadink.cn/)
广告列表对标页：[https://ad.leadink.cn/DmvTqXBpfF.php/ad?ref=addtabs](https://ad.leadink.cn/DmvTqXBpfF.php/ad?ref=addtabs)
技术栈：FastAPI + SQLAlchemy + PostgreSQL + HTML/CSS/JavaScript

## 1. 文档目的

本文档用于统一产品、研发、测试、部署和运营人员对整套后台的需求理解，作为整站开发、联调、验收和后续变更的基础文档。

本项目以目标站 `https://ad.leadink.cn/` 的页面结构、菜单层级、字段、筛选、按钮、状态文案、弹窗、分页、导出、审核和风控行为作为复刻基准。未在当前登录态逐页确认的内容，统一标记为“待对标确认”，并记录到 `docs/benchmark.md`。

## 2. 项目目标

建设一套自研的广告会员运营后台，覆盖以下业务：

- 后台登录与权限管理
- 仪表盘总览
- 主体/代理商管理
- 游戏管理
- 会员管理
- 广告列表、统计、导入、导出和告警
- 用户提现审核
- 补贴申请审核
- 金币流水查询
- 白名单、风控历史和设备风控
- 个人资料
- 养机教程

目标不是复制第三方源码或私有资源，而是在业务结果、页面结构和交互体验上实现等价复刻。

## 3. 复刻原则

### 3.1 必须对齐的内容

- 菜单名称和菜单层级
- 页面路由和入口
- 表格字段顺序和显示名称
- 筛选项、默认值、联动规则和重置行为
- 工具栏按钮、弹窗和表单布局
- 状态值、状态颜色和状态文案
- 分页、刷新、空数据、异常和成功反馈
- 详情页、审核流和导出行为
- 角色权限和按钮可见性

### 3.2 自研边界

- 不直接复制目标站源码、私有接口、生产数据、密钥或未授权资源。
- 后端、数据库和前端都采用本项目自有实现。
- 目标站字段名与本地字段名不一致时，接口需要提供兼容字段或映射，保证业务口径一致。
- 视觉还原只做功能等价，不强行复用第三方品牌资源。

### 3.3 当前确认状态

- 已确认：广告列表的核心字段、`is_fu / fu_type / is_look / request_id / trans_id` 口径、广告类型归并规则。
- 待确认：除广告模块外的逐页字段顺序、按钮文案、弹窗内容、部分状态值和权限细节。
- 现有工程已具备：主体、游戏、会员基础 CRUD，广告导入导出、统计、告警，提现和补贴审核，金币流水和风控列表。

## 4. 范围说明

### 4.1 本期范围

- 登录与会话
- 仪表盘
- 主体管理
- 游戏管理
- 会员管理
- 广告列表
- 广告统计
- 广告导入记录
- 广告告警
- 提现审核
- 补贴申请
- 金币流水
- 白名单
- 风控历史
- 设备风控
- 个人资料
- 养机教程

### 4.2 不在范围

- 不复用第三方源码、图片和私有接口
- 不做外部开放 API
- 不做移动端 App
- 不做与目标站无关的营销页或官网页

## 5. 信息架构

### 5.1 页面结构

- 顶部栏
- 左侧菜单
- 多标签页
- 内容区
- 弹窗
- 提示消息

### 5.2 菜单结构

| 分组 | 菜单项 | 页面类型 |
|---|---|---|
| 运营 | 仪表盘 | 汇总页 |
| 运营 | 主体管理 | 列表 + 表单 |
| 运营 | 游戏管理 | 列表 + 表单 |
| 运营 | 广告列表 | 列表 + 详情 |
| 运营 | 会员管理 | 列表 + 详情 |
| 运营 | 用户提现 | 审核列表 |
| 运营 | 补贴申请 | 审核列表 |
| 收益管理 | 金币流水 | 明细列表 |
| 风控管理 | 白名单 | 列表 + 开关 |
| 风控管理 | 风控历史 | 日志列表 |
| 风控管理 | 设备风控 | 设备列表 |
| 常规管理 | 个人资料 | 表单 |
| 常规管理 | 养机教程 | 内容页 |

### 5.3 通用交互

- 所有列表默认分页。
- 所有筛选提交后回到第一页。
- 所有成功操作后刷新当前列表。
- 所有敏感操作需要确认弹窗。
- 所有错误都要显示可读原因。
- 宽表格必须支持横向滚动。

## 6. 角色与权限

| 角色 | 权限范围 |
|---|---|
| superadmin | 全部菜单和全部操作 |
| operator | 主体、游戏、会员、广告查看/导入/导出、仪表盘、个人资料 |
| reviewer | 提现审核、补贴审核、广告和会员只读 |
| risk | 白名单、风控历史、设备风控，只读业务数据 |

权限控制必须在后端生效，前端隐藏按钮只能作为辅助。

## 7. 功能需求

### 7.1 登录与会话

#### 页面功能

- 账号输入
- 密码输入
- 登录按钮
- 登录失败提示
- Token 失效后回到登录页

#### 规则

- 账号存在、状态启用且密码正确时允许登录。
- 登录成功后返回 Bearer Token、过期时间和当前用户信息。
- 密码不得明文存储。
- 生产环境必须使用强密钥和强密码策略。

### 7.2 仪表盘

#### 展示内容

- 主体数
- 游戏数
- 会员数
- 待审提现数
- 待审补贴数
- 风控记录数
- 最近业务活动

#### 交互

- 进入后台默认展示仪表盘或上次打开页面。
- 指标卡可跳转到对应模块。
- 加载失败时显示重试入口。

### 7.3 主体管理

#### 页面能力

- 列表
- 搜索
- 新增
- 编辑
- 删除
- 详情

#### 关键字段

- 主体名称
- 登录账号
- 上级主体
- 状态
- 游戏广告开关
- 更新时间

#### 规则

- 主体名称必填。
- 上级主体不能形成循环关系。
- 有关联数据时删除应被阻止。
- 停用逻辑是否联动下级模块，待对标确认。

### 7.4 游戏管理

#### 页面能力

- 列表
- 搜索
- 新增
- 编辑
- 删除
- 详情

#### 关键字段

- 游戏名称
- 游戏标识
- 所属主体
- 游戏状态
- 广告状态
- 游戏图标
- 更新时间

#### 表单要点

- 游戏名称、标识、地址、图标
- 主体关联
- 开关配置
- 抽奖、奖励池、兑换和提现参数
- 扩展 JSON 配置

### 7.5 会员管理

#### 页面能力

- 列表
- 搜索
- 新增
- 编辑
- 删除
- 详情
- 白名单开关

#### 关键字段

- 会员账号
- 昵称
- 主体/游戏
- 金币余额
- 冻结金币
- VIP
- 状态
- 白名单
- 设备 ID

#### 规则

- 会员账号必填。
- 游戏必须属于所选主体。
- 删除时要检查广告、提现和流水关联。
- 金币变更必须可追踪。

### 7.6 广告列表

广告列表是本项目的核心页面，目标入口为：
`/DmvTqXBpfF.php/ad?ref=addtabs`

#### 页面布局

- 顶部栏
- 左侧菜单
- 筛选区
- 指标区
- 表格区
- 分页区
- 详情弹窗

#### 列表字段

| 展示列 | 数据字段 | 说明 |
|---|---|---|
| Id | `id` | 记录主键 |
| 上级Id | `parent_id` / `user.parent_id` | 上级标识 |
| 上级支付宝姓名 | `parent_payment_name` / `receive_name_parent` | 上级收款姓名 |
| 会员ID | `user_id` | 会员标识 |
| 用户账号 | `user_account` / `user.username` | 用户账号 |
| 支付宝姓名 | `receive_name` | 收款姓名 |
| 游戏名称 | `game_name` / `game.name` | 游戏名称 |
| ECPM | `pre_ecpm` / `ecpm` | 展示数值 |
| 金币 | `estimate_income` / `coin` | 展示数值 |
| 广告平台 | `ad_network_platform_name` | 平台名称 |
| 抽奖 | `is_lottery` | 是/否 |
| 任务 | `is_rw` | 是/否 |
| 奖励类型 | `is_type` / `reward_type` | 类型文案 |
| 广告类型 | `is_fu` | 只显示激励/副广 |
| 副广类型 | `fu_type` | 副广子类型 |
| 状态 | `is_look` / `status` | 成功/失败 |
| 观看时间 | `watched_at` | 日期时间 |
| 广告代码位 | `ad_network_rit_id` / `ad_code` | 代码位 |
| 广告 request_id | `request_id` | 请求链路 |
| 交易 trans_id | `trans_id` | 交易链路 |

#### 广告类型规则

- 一级广告类型只有 `激励` 和 `副广` 两个选项。
- `is_fu = 0` 显示为 `激励`。
- `is_fu = 1` 显示为 `副广`。
- `副广` 统一合并插屏、开屏、横幅/Banner、信息流。
- `fu_type` 映射：1 开屏，2 Banner，3 插屏，4 信息流。

#### 筛选项

- 上级 ID
- 会员 ID
- 游戏名称或游戏 ID
- 主体/代理商名称或 ID
- 金币区间
- 广告平台
- 广告类型
- 副广类型
- 状态
- 观看时间范围
- 关键词

#### 业务规则

- `request_id` 原则上唯一。
- `request_id` 缺失、重复、冲突都必须提示。
- 成功记录但金币或 ECPM 为 0 时仍保留并进入告警。
- 导出数据必须和当前筛选条件一致。

### 7.7 广告统计

#### 功能

- 按日期聚合
- 按广告分组聚合
- 按游戏聚合
- 按主体/代理商聚合

#### 指标

- 记录数
- 用户数
- ECPM 合计
- 平均 ECPM
- 金币合计
- 预估收益合计

### 7.8 广告导入、导出和导入记录

#### 导入

- 支持 CSV
- 支持 UTF-8、UTF-8 BOM、GBK/GB18030
- 支持中英文表头别名
- 保存导入批次
- 保存失败行
- 保存操作人和文件名

#### 必须校验

- `request_id` 不能为空
- 同文件内 `request_id` 不能重复
- 已存在 `request_id` 不能重复导入
- 数字字段必须可解析
- 观看时间必须可解析
- 广告分组必须归一为激励或副广，内部实现可保留兼容映射

#### 导出

- 导出使用当前列表筛选条件
- 导出文件采用 CSV
- 导出数量需要上限控制

### 7.9 广告告警

#### 告警类型

- 请求 ID 重复
- 请求 ID 缺失
- 金币为零
- ECPM 为零
- 分组异常
- 导入失败

#### 告警内容

- 告警级别
- 告警类型
- 摘要
- 来源类型
- request_id
- 行号
- 文件名
- 操作人
- 发生时间

#### 操作

- 按条件筛选
- 查看详情
- 跳转原广告
- 跳转导入失败明细

### 7.10 用户提现

#### 页面能力

- 提现列表
- 按状态筛选
- 查看详情
- 通过
- 驳回
- 标记转账

#### 状态

- 待审核
- 已通过
- 已驳回
- 待转账
- 已标记
- 转账失败

#### 规则

- 待审核才能通过或驳回。
- 已通过且未转账才能标记转账。
- 驳回必须填写原因。
- 重复审核和重复转账必须阻止。

### 7.11 补贴申请

#### 页面能力

- 补贴列表
- 按状态筛选
- 查看详情
- 通过
- 驳回

#### 规则

- 待审核才能处理。
- 驳回必须填写原因。
- 审核结果需要可追溯。

### 7.12 金币流水

#### 页面能力

- 只读列表
- 搜索
- 分页

#### 字段

- 流水 ID
- 用户 ID
- 主体 ID
- 游戏 ID
- 变更前金币
- 变更值
- 变更后金币
- 类型
- 备注

#### 规则

- 金币流水不可直接编辑或删除。
- 变更前、变更值、变更后必须可核对。

### 7.13 白名单

#### 页面能力

- 白名单列表
- 搜索
- 加入白名单
- 移出白名单

#### 规则

- 操作需要记录时间和操作人。
- 白名单与风控逻辑的实时关系待目标站对标确认。

### 7.14 风控历史

#### 页面能力

- 日志列表
- 按用户、标签、设备、IP、动作和风险等级搜索
- 详情查看

#### 字段

- 标签编码
- 标签描述
- 设备主标识
- IP
- 动作
- 风险等级
- 风险分数

### 7.15 设备风控

#### 页面能力

- 设备列表
- 搜索
- 详情查看
- 处置动作按目标站确认

### 7.16 个人资料

#### 页面能力

- 查看当前账号
- 修改昵称或显示名称
- 修改密码

#### 规则

- 修改密码必须验证当前密码。
- 新密码至少 10 位。

### 7.17 养机教程

该模块目前只确认有菜单入口，后续需要逐页确认：

- 页面内容结构
- 标题、正文、图片和附件
- 查看和编辑权限
- 排序和发布状态

## 8. 数据模型

详细表结构见 `docs/db.md`，本章只给出核心对象边界。

### 8.1 核心表

- `admin_users`
- `agents`
- `games`
- `members`
- `ad_records`
- `ad_import_batches`
- `ad_import_errors`
- `withdrawals`
- `subsidies`
- `coin_logs`
- `risk_records`

### 8.2 关联关系

- 一个主体可关联多个游戏。
- 一个游戏属于一个主体。
- 一个会员关联主体和游戏。
- 广告记录关联会员、游戏、主体和请求链路。
- 提现和补贴关联会员，并保留主体、游戏快照。
- 金币流水和风控记录都要保留可追踪的来源信息。

### 8.3 广告核心字段

广告记录至少需要覆盖以下字段口径：

`id / parent_id / parent_payment_name / user_id / user_account / agent_id / game_id / game_name / receive_name / ecpm / pre_ecpm / coin / estimate_income / ad_network_platform_name / is_lottery / is_rw / reward_type / ad_type / sub_ad_type / ad_group / is_type / is_fu / fu_type / is_look / status / watched_at / create_time / ad_code / ad_network_rit_id / request_id / trans_id`

## 9. API 需求

接口详细定义见 `docs/api.md`。本章只描述总体要求。

### 9.1 统一约定

- 登录接口前缀：`/api/auth/*`
- 业务接口前缀：`/api/v1/*`
- 鉴权方式：`Authorization: Bearer <token>`
- 列表统一返回 `total / items / limit / offset`
- 统计接口返回 `summary` 和聚合数组
- 参数错误返回 422，未登录返回 401，无权限返回 403，不存在返回 404

### 9.2 主要接口

- `POST /api/auth/login`
- `GET /api/auth/me`
- `POST /api/auth/change-password`
- `GET /api/v1/dashboard/summary`
- `GET /api/v1/dashboard/activity`
- `GET /api/v1/agents`
- `POST /api/v1/agents`
- `PATCH /api/v1/agents/{id}`
- `DELETE /api/v1/agents/{id}`
- `GET /api/v1/games`
- `POST /api/v1/games`
- `PATCH /api/v1/games/{id}`
- `DELETE /api/v1/games/{id}`
- `GET /api/v1/members`
- `POST /api/v1/members`
- `PATCH /api/v1/members/{id}`
- `DELETE /api/v1/members/{id}`
- `GET /api/v1/ads`
- `GET /api/v1/ads/{id}`
- `GET /api/v1/ads/statistics`
- `POST /api/v1/ads/import`
- `GET /api/v1/ads/imports`
- `GET /api/v1/ads/imports/{batch_id}/errors`
- `GET /api/v1/ads/alerts`
- `GET /api/v1/ads/export`
- `GET /api/v1/withdrawals`
- `PATCH /api/v1/withdrawals/{id}`
- `POST /api/v1/withdrawals/{id}/approve`
- `POST /api/v1/withdrawals/{id}/reject`
- `POST /api/v1/withdrawals/{id}/transfer`
- `GET /api/v1/subsidies`
- `PATCH /api/v1/subsidies/{id}`
- `POST /api/v1/subsidies/{id}/approve`
- `POST /api/v1/subsidies/{id}/reject`
- `GET /api/v1/coin-logs`
- `GET /api/v1/risk/history`
- `GET /api/v1/risk/whitelist`
- `GET /api/v1/risk/devices`

## 10. 非功能需求

### 10.1 性能

- 常规列表接口要支持分页查询。
- 导入和导出需要设置数量上限。
- 统计接口必须避免全表无控制扫描。
- PostgreSQL 需要为高频查询字段建立索引。

### 10.2 安全

- 业务接口必须登录后访问。
- 密码不得明文保存。
- JWT 密钥、数据库密码和管理员密码必须通过环境变量注入。
- 生产环境必须禁用演示数据自动初始化。
- 日志不能记录密码、Token 和完整敏感信息。

### 10.3 可用性

- 支持常用桌面浏览器。
- 宽表格必须横向滚动。
- 空状态、错误状态、加载状态要明确。
- 关键操作必须留下操作者和时间戳。

### 10.4 可维护性

- 数据库变更使用 Alembic 迁移。
- 页面与接口通过字段契约对齐。
- 业务规则尽量集中在后端。
- 需求确认结果写入 `docs/benchmark.md`。

## 11. 测试与验收

### 11.1 功能测试

- 登录成功、失败和过期
- 四类角色权限
- 主体、游戏、会员 CRUD
- 广告列表字段和筛选
- 广告详情、统计、导入、导出和告警
- 提现通过、驳回和转账
- 补贴通过和驳回
- 白名单开关
- 风控列表查询

### 11.2 数据测试

- 空值和非法数字
- 非法日期
- 主体循环关系
- 游戏主体不匹配
- 会员游戏不匹配
- 关联数据删除保护
- request_id 重复

### 11.3 验收标准

- 能正常登录并进入后台。
- 菜单、页面和操作入口与目标站一致或等价。
- 广告列表字段、筛选、广告类型口径和详情一致。
- 导入导出、统计和告警行为正确。
- 主体、游戏、会员、提现、补贴和风控模块可正常使用。
- 单测和基础语法检查通过。

## 12. 开发周期预估

| 阶段 | 内容 | 预估 |
|---|---|---:|
| 对标盘点 | 登录目标站逐页确认菜单、字段、筛选、按钮和状态 | 2-3 天 |
| 需求冻结 | 冻结字段字典、状态字典、权限矩阵和接口契约 | 1-2 天 |
| 数据建模 | PostgreSQL 表结构、索引和迁移 | 2-4 天 |
| 核心后端 | 认证、主体、游戏、会员、广告、统计、导入导出 | 5-8 天 |
| 审核风控后端 | 提现、补贴、金币流水、白名单、风控接口 | 3-5 天 |
| 前端复刻 | 全局布局、列表、筛选、弹窗、反馈和分页 | 5-8 天 |
| 联调回归 | 权限、边界、异常、兼容和数据校验 | 2-4 天 |
| 部署上线 | 环境变量、迁移、日志、备份和上线检查 | 1-2 天 |

预计合计：21-36 个工作日。

## 13. 交付物

- 完整需求规格说明书
- 页面对标清单
- FastAPI 后端服务
- SQLAlchemy 数据模型
- PostgreSQL / Alembic 迁移脚本
- 前端管理后台
- API 接口文档
- 数据库设计文档
- 自动化测试
- Docker 部署配置

## 14. 待确认事项

以下事项需要在目标站登录态下逐页核对，并补充到 `docs/benchmark.md`：

1. 仪表盘的精确指标和是否存在趋势图。
2. 主体、游戏、会员页面的字段顺序、默认筛选和按钮文案。
3. 提现和补贴的完整状态字典及按钮文案。
4. 白名单、风控历史和设备风控的逐页交互。
5. 个人资料和养机教程的具体页面内容。
6. 是否存在列显隐、批量操作、自定义分页等高级能力。
7. 审计日志和登录日志是否纳入本期。

确认结果以更新后的需求文档和 `docs/benchmark.md` 为准。
