# 复刻验收记录

## 已验证

- 后端入口 `backend/app/main.py` 已切换到恢复源码，原 `main.pyc` 保留。
- 25 项业务测试直接导入恢复源码并通过（2026-09-12）；此前另有 14 项会员查询断言通过。
- 浏览器已验证登录、会员账号查询、筛选值保留、重置、VIP 和状态开关渲染。
- `visual-baseline/audit/manifest.json` 记录 13 个本地路由的加载结果与对应截图。加载通过不等于功能或视觉一致。

## 基准纠正

- 原 `visual-baseline/reference/dashboard.png` 是会员管理页。
- 原 `page-1.png` 至 `page-3.png` 是重复会员页，`page-4.png` 是站点首页，`page-5.png` 是个人资料。
- 原 `page-6.png` 是成功提示，`page-7.png` 至 `page-19.png` 是错误提示，不能作为对应管理页面基准。
- 新基准位于 `visual-baseline/reference-verified`，按页面名称映射。实际截图包含侧栏与内容 iframe；逐页核实 iframe 的内容和选中菜单后才能作为有效基准。

## 未验收

- 设备风控现有本地实现错误地使用风险日志。新取回的 `visual-baseline/reference-verified/device-controller.js` 证明对标使用会员字段、金币余额和单 APP／多 APP 行为入口；后端入选规则尚无证据，不能将当前设备页标为完成。
- 提现／补贴默认申请时间已改为北京时间当天及前两天，重置恢复此默认值；浏览器已验证。批量补贴审批有后端事务测试，但完整批量操作、编辑弹窗和汇总口径仍未验收。

- 会员页游戏、代理商选择器与日期选择交互、支付宝姓名、工具栏及弹窗仍有差异。
- 历史会员没有注册 IP 的记录保持空值，不用最后登录 IP 替代。
- 其他管理页面需要依据重新采集的有效基准逐页实现和验收。
- 本地数据量与对标不同，不能以填充硬编码行数作为视觉完成证据。
- 不使用截图数量、接口 200 或语法检查来宣称完成一比一复刻。

## 本轮源码修复（2026-09-12）

- 修复补贴单条驳回错误发送 `reason` 的问题，改为后端要求的 `message`；提现仍发送 `reason`。空原因在前端拦截，同一行提交期间禁用两个审核按钮。
- 恢复提现批量拒绝的非空原因校验；独立数据库测试验证空原因不更新、混合已审记录整批回滚、正常拒绝记录原因与审核人。
- 修复提现 PATCH 审核与转账条件分支错位；验证先审核后转账、禁止回退审核与转账状态。提现与补贴 PATCH 将外部失败状态 4 统一存为内部状态 2，且必须填写拒绝原因，避免失败列表漏查。
- 无效日期不再销毁筛选表单；过期失败请求不覆盖新结果。
- `npm run check` 自动发现并检查全部 public JavaScript 及服务、检查脚本，本轮共 15 个文件通过。
- `verify_review_regressions.py` 使用模拟接口验证两种驳回请求、空原因拦截、图片渲染及危险协议过滤、日期纠错和过期请求隔离；截图 `visual-baseline/verified/review-regression-fixture.png` 是测试夹具，不是对标数据或视觉一致性证据。
- `verify_review_filters.py` 在本地服务验证提现、补贴查询／重置／失败状态筛选通过。上述结果不代表设备风控、代理商／游戏配置等未验收部分已完成。

## 提现字段对标推进（2026-09-12）

- 直接读取公开资源 `https://ad.leadink.cn/assets/js/backend/tixian.js`，HTTP 200，保存到 `visual-baseline/reference-verified/withdrawals-controller.js`。控制器与已有具名截图共同确认字段及显示规则。
- 新增 `good_name`、`device_manufacturer`、`check_status_txt` 存储字段及 Alembic 迁移 `b912e6d41a30`；历史数据默认空字符串，不推测商品或审查结论。独立临时数据库完成整条迁移链至旧版本、新版本升级和回退验证。
- 商品名称按包含关系筛选，百分号作为字面字符；状态筛选与状态标签联动，重置清空状态。新增后端测试验证商品过滤与字段序列化。
- 提现金额按对标控制器的原始 `exchange_value / 10` 显示为元；失败原因列改读 `sub_msg`，`reason` 仍独立保存为拒绝原因。新增商品、手机型号、作弊审查列，并将操作列移至申请时间之前。
- 浏览器模拟数据验证 125 显示为 12.5 元、VIP／提现方式／定时状态文本及字段映射；本地服务筛选测试通过并更新截图。
- 清理 `subsidies.css` 对提现共享样式的重复覆盖，修复状态标签和筛选区域多余垂直间距；空列表隐藏分页。
- 仍缺用户行为入口、选择列及完整工具栏（编辑、批量拒绝、实际支付、黑名单、列控制、导出、排序）和汇总口径。对标控制器还显示“拒绝”和“有理由拒绝”为两个独立动作，当前本地只有有理由拒绝；上一轮的必填原因校验不能视为对标已完成。支付接口目前仅修改状态，不等价于对标实际转账。

## 提现操作对标推进（2026-09-12）

- `inspect_reference_withdrawal_forms.py` 登录后仅 GET 提现列表及理由表单地址；对标列表返回 `total: 0`，没有可用于核实编辑弹窗的记录，理由地址也未返回表单。结果保存于 `withdrawals-forms.json`，未提交任何对标业务操作，编辑弹窗仍未核实。
- 根据 `withdrawals-controller.js` 已确认的无理由拒绝动作，新增普通拒绝与批量拒绝接口；原有带理由拒绝保持独立且仍校验原因。两种拒绝均保存内部失败状态 2、对外状态 4 和审核人。
- 提现页新增选择列、本页全选／半选状态、批量拒绝按钮；普通拒绝、同意、批量拒绝使用可取消的确认弹窗。提交期间禁用批量按钮，切换页面后不再提交旧页面确认的操作。
- ID、金额、申请时间表头可切换排序；后端对白名单排序字段执行数据库排序后分页，默认 ID 倒序。新增跨页排序断言，避免只排序当前页。
- 18 项独立数据库测试通过；新增覆盖无理由拒绝权限、重复拒绝、空选择、混合状态整批回滚与跨页排序。浏览器夹具验证普通拒绝／批量拒绝取消与确认、选中项请求体、排序参数及表头状态；本地查询／重置／状态回归通过。JavaScript 语法检查 16 文件通过。
- 上一节所列的选择列、批量拒绝和三项可见列排序已有实现；编辑、用户行为、实际支付、黑名单、列控制、导出、汇总及整页视觉仍未完成验收，目标保持未完成。

## 提现列控制推进（2026-09-12）

- 根据对标控制器的 `visible: false` 字段补入上级 ID、上级账号、代理商名称、昵称、收货地址、拒绝原因、更新时间，默认隐藏；提供工具栏列菜单，同时控制表头和数据单元格。
- 审核列表从真实会员、上级会员、代理商关联返回显示字段，缺失关联保持空值。新增独立数据库测试验证提现及补贴的关联字段。
- 列选择在当前会话的筛选、刷新、排序、分页重绘后保留；至少保留一列，空表 colspan 随显示列调整，选择列始终保留。菜单支持 Escape 和外部点击关闭。
- 更新时间列启用已有后端排序。19 项后端测试、16 文件 JavaScript 检查通过；浏览器夹具验证显示／隐藏、刷新／筛选／分页保留、最后一列保护、空表跨度。本地提现／补贴筛选回归通过。
- 对标还包含快递名称、快递单号、备注及内部号等隐藏字段，目前本地尚未完整建模；列菜单并未达到全部字段一致。用户行为、编辑、支付、黑名单、导出、汇总、视觉差异与其他管理页仍属未完成范围。

## 设备入选规则核实与剩余隐藏字段（2026-09-12）

- 新增只读脚本 `inspect_reference_device_scope.py`，仅记录字段名称、总数和标记分布，不保存账号或设备标识。实际对标设备风控返回 0 条、会员列表返回 7 条且 `device_id_ban` 全部为 0，证据保存于 `device-scope.json`。空正样本不足以证明设备风控入选规则，现有设备页仍未纠正，不能据此标记完成。
- 提现新增快递名称、快递单号、备注字段，会员新增可空内部号字段；审核列表返回这些真实字段。未知内部号为空，不当作“否”；快递单号为字符串，保留前导零。
- 四字段按对标默认隐藏，可在列菜单启用。新增迁移 `c912f7d52b41`，独立临时数据库完整升级／回退通过；本地开发库通过兼容初始化增加字段。
- 19 项后端测试通过（关联字段用例扩充），16 个 JS 文件语法检查通过；浏览器验证隐藏默认值、显示数据、前导零及备注 HTML 转义，原审核筛选回归通过。
- 提现数据列中此前遗漏的四个隐藏字段已补入；用户行为操作列、完整编辑／支付／黑名单、导出、汇总和视觉仍未完成。设备风控和其他管理页也继续保留未完成状态。

## 补贴字段与批量操作推进（2026-09-12）

- 读取 `https://ad.leadink.cn/assets/js/backend/butie.js` 并保存为 `subsidies-controller.js`，确认收件人／联系方式字段、金额与时间排序、失败原因列、默认隐藏列及批量拒绝只提交 ID 的行为。
- 补贴模型新增独立收件人和联系方式，列表支持精确筛选；不使用会员昵称或账号推测收件人。迁移 `d912a8e63c52` 在独立临时数据库升级／回退通过。
- 补贴页新增收件人／联系方式列与筛选，状态下拉与标签联动；失败原因读 `sub_msg`，操作列调整到申请时间前。金额、申请时间和更新时间支持数据库排序后分页；隐藏列菜单复用审核表格实现。
- 批量同意／拒绝使用确认弹窗，拒绝调用新的无理由接口；本页所有记录可选，后端对混入已审记录整批回滚。旧带理由拒绝接口保留独立校验。前端确认后检查页面与请求代次，提交期间禁用勾选和批量按钮。
- 20 项后端测试、16 个 JS 文件检查通过；浏览器夹具验证补贴筛选、排序、拒绝取消与确认请求体，两个审核页面实际筛选回归通过。
- 对标补贴行操作来自编辑／删除，当前本地仍有单条审核按钮，操作区尚不一致；用户行为、编辑表单、删除、汇总、图片查看弹窗等仍待实现或验收。项目整体一比一复刻保持未完成。

## 补贴申请图片预览（2026-09-12）

- 读取并保存对标 `assets/js/require-table.js` 为 `table-controller.js`，确认图片点击调用 `Layer.photos`，数据限于当前行图片数组、起点为所点击缩略图的位置。
- 本地改为页内相册：按当前行分组，点击位置起始、前后切换及序号、图片加载失败提示、Esc／关闭按钮／遮罩关闭，切换页面关闭并移除监听。保留 HTTP／HTTPS 协议限制；图片异步事件带代次保护，旧图完成不会覆盖新图状态。
- `verify_subsidy_gallery.py` 使用模拟图片验证行隔离、起点、切换、失败恢复、无新标签页、关闭与路由清理，通过。`subsidy-gallery-fixture.png` 为测试夹具截图，不代表真实申请图片或对标像素一致。
- 16 个 JS 文件语法检查、审核页面浏览器回归通过。后端未修改，本轮未重复后端测试。
- 图片相册交互已有实现，真实资源可用性、对标 Layer 的尺寸／动画／工具条等细节仍需核对，不能据此将整个补贴页或项目标记完成。

## 审核筛选区布局与折叠（2026-09-12）

- `inspect_reference_review_layout.py` 实际打开两个对标页面，采集筛选控件矩形、工具栏按钮，并点击“普通搜索”验证折叠／展开；仅操作展示控件，无业务写入。证据位于 `withdrawals-layout.json`、`subsidies-layout.json`。
- 按实测修正四列筛选宽度、33px 控件高度、48px 行距及 41px 标签高度，补贴提交按钮移至第二列。新增“普通搜索”工具栏按钮，折叠不清除输入，提供展开状态与关联表单标识。
- `verify_review_layout.py` 登录本地实际服务，在同一 1920×1080 视口逐项比较两个页面的输入框 x／y／宽／高，容差 1px，全部通过；并验证未提交输入在折叠／展开后保留。截图保存为 `withdrawals-layout.png`、`subsidies-layout.png`。
- 16 个 JS 文件检查和审核交互夹具回归通过。此证据仅覆盖桌面筛选输入框几何与折叠交互，不证明标签字体、选择器／日期弹窗、表格、工具栏全部按钮或整页视觉已经一致。

## 审核表格／卡片视图（2026-09-12）

- 对标共用配置启用 `showToggle`；读取公开 Bootstrap Table JS／CSS 并保存为 `bootstrap-table.js`、`bootstrap-table.css`，确认卡片布局按记录纵向展示字段名和值、标题最小宽度 30%、隐藏空值。
- 两个审核页新增切换按钮，卡片与表格复用原数据节点，使勾选、审核按钮和图片相册事件保持有效；当前页刷新后保留视图，列选择继续适用。
- 浏览器夹具验证卡片下空字段隐藏、单行选择、图片预览、切回表格保留选择、提现金额显示及刷新保持视图。夹具截图为 `subsidy-card-fixture.png`，不作为真实对标数据证据。
- JavaScript 16 文件语法检查、审核夹具回归、两页实际查询／重置／状态检查通过；两页筛选区矩形比较仍通过。未修改后端。
- 卡片具体字体、间距及全量记录视觉尚未像素验收；其余编辑／用户行为／支付／黑名单／导出／汇总和其他页面差异仍然存在，整体目标保持未完成。

## 审核导出（2026-09-12）

- 对标共用配置确认六格式 `json/xml/csv/txt/doc/excel`，`exportDataType: auto` 在有勾选时导出所选，否则导出全部筛选结果；排除选择列与操作列。扩展源文件保存为 `bootstrap-table-export.js` 和 `table-export.js`。
- 两页接入导出菜单，使用表格当前可见字段及显示格式；无勾选时按当前已提交筛选、排序每批读取 200 条直至总数，有勾选时仅使用对应记录。途中总数变化、空批次或重复 ID 时提示刷新，避免已检测到的不完整结果被下载；这不构成数据库一致性快照保证。
- 使用上游 tableExport 1.10.26 与 jQuery 3.6.0 本地依赖，保留 MIT 许可和来源说明。对标 jQuery 地址返回第三方脚本加载代码，未执行该文件；已改用上游发布版。`noConflict()` 避免覆盖现有 `$`。
- `verify_review_export.py` 验证 205 条跨页结果、筛选和排序参数保留、所选记录不请求全量、隐藏列和操作列排除、金额显示、六种文件扩展名及 JSON／XML／CSV 内容结构、补贴所选导出。所有下载保存在临时目录，使用模拟业务数据。
- 19 个 JS 文件语法检查及审核回归通过；桌面筛选几何比较保持通过。本轮未修改后端。
- 导出核心流程已有实现，但真实大数据量、并发变化及所有文件在 Office 中的外观未验收。完整编辑／用户行为／支付／黑名单／汇总及其余页面仍未完成；目标保持未完成。

## 审核分页（2026-09-12）

- 对标共用代码明确 `pageList: [10,15,20,25,50]`，默认 `Config.pagesize || localStorage.pagesize || 10`，切换时记忆 `pagesize`。本次在线核实页面级配置因 iframe 跳转到其他页面失败，未覆盖原布局证据；页面级默认值尚未确认。
- 本地接入五种条数、页码跳转、记录区间、前后页禁用与本地记忆，使用已确认的共用回退默认 10。切换条数回第一页，记录减少后自动跳至有效末页，避免留在空的越界页。
- 205 条模拟数据验证 50 条分页的第五页仅 5 行、返回第四页、切回 10 条和记忆值；在第 21 页将数据减至 3 条后刷新回到第一页。导出全部／所选及六格式测试仍通过。
- 19 个 JS 文件检查、审核交互回归、两个实际页面的筛选区尺寸验证通过。本轮未修改后端；分页外观与页面级默认值仍待对标最终核实，项目整体未完成。

## 审核日期选择器（2026-09-12）

- 保存对标 `require-form.js` 和日期组件源码，确认 daterangepicker 2.1.27、无时间选择、自动应用、自定义日期及今天／昨天／最近 7 天／最近 30 天／本月／上月快捷范围。使用同版本上游日期组件、本地 Moment 2.29.4，MIT 许可随依赖保存。
- 两个审核页申请时间和更新时间输入框接入日历，沿用 `YYYY-MM-DD HH:mm:ss - YYYY-MM-DD HH:mm:ss`；快捷范围以北京时间生成，手动输入保持可用，打开空输入不自动写值。当前组件配置隐藏清空按钮，仍可手动清空；保留对标 cancel 事件清空逻辑。
- 日期与导出共用 jQuery 加载 promise，避免并行加载覆盖 `$`；移除筛选表单后清理日期组件及其事件，避免重复弹窗。
- `verify_review_dates.py` 在洛杉矶浏览器时区、固定北京时间跨日时刻验证今天、最近 7 天、上月、自定义月内区间、独立更新时间、手动清空和节点移除清理通过。
- 22 个 JS 文件语法检查、审核回归、导出回归、实际审核筛选测试及输入框矩形比较通过。日历字体／定位／中文文案与对标现场仍未完整视觉验收；其余项目差异保持未完成。

## 主体列表开始对标（2026-09-12）

- 读取 `assets/js/backend/agent.js`，保存为 `agents-controller.js`，确认默认名称／状态／创建时间、日期筛选和排序，账号／上级列在控制器中已注释，不应作为默认列。
- 新增 `agents.js/css` 独立列表并接入原路由，移除该页通用表格的错误默认字段，保留现有编辑／删除事件入口；名称包含筛选、状态、创建／更新时间区间、ID／创建时间排序以及本页选择接入真实 API。
- 后端新增名称字面包含过滤、时区归一化日期边界、白名单排序。21 项后端测试通过；浏览器验证实际主体路由、空结果、重置、勾选与排序，无业务数据写入；23 个 JS 文件检查通过。截图 `agents-list.png`。
- 此页仍缺对标游戏管理列中的 OSS 配置及主体范围“进入”页、穿山甲隐藏字段、完整新增／编辑／删除视觉、通用工具栏和精确分页等。当前保留编辑／删除行为不代表弹窗已复刻。整体目标保持未完成。

## 主体 OSS 配置（2026-09-12）

- 只读获取实际主体 OSS 表单，字段结构保存于 `agents-forms.json`；确认 ossKey、ossKeySecret、endPoint、bucket 及确定／重置按钮，未保存字段值或对标密钥。普通编辑地址未取得表单，仍未核实。
- 新增独立 OSS GET／PATCH 接口和存储字段，允许 superadmin／operator，其他角色禁止；主体列表和详情不包含 OSS 密钥。配置读写不执行外部上传请求。
- 主体列表新增游戏管理列中的 OSS 配置按钮及弹窗，重置恢复本次打开时的值，保存失败显示错误，路由切换关闭弹窗。
- 22 项后端测试通过，验证保存后读取、角色限制、列表／详情不泄漏配置；浏览器以模拟配置验证打开／修改／重置／保存请求体；独立数据库迁移 `e912b9f74d63` 升级和回退通过。23 个 JS 文件检查通过。
- OSS 配置字段交互已有实现，真实 OSS 上传集成、弹窗精确尺寸及完整游戏管理入口仍未验收。项目整体保持未完成。

## 主体范围统计入口（2026-09-12）

- 读取主体统计控制器及实际页面，证据保存为 `agent-dash-controller.js`、`agent-dashboard.json/png`。确认“进入”指向主体仪表盘，包含主体名称、今日新增／登录、31 天注册趋势以及数据分析／游戏列表／用户列表／提现子导航，不是直接进入游戏列表。
- 主体列表为无上级主体显示“进入”，按控制器的 parent_id 条件；接入 `agent-dashboard` 路由并在当前会话保存所选主体。新增主体统计接口，以 Member.agent_id 隔离，按北京时间分日、补齐空日期；登录人数统计今日最后登录的会员，忽略无法解析的历史时间。
- 23 项后端测试通过，新增跨北京时间午夜、不同主体隔离、31 天日期序列测试；浏览器验证真实入口、31 个图表点和刷新恢复；23 个 JS 文件语法检查通过。
- 本地目前仅数据统计部分，四个子页面还未实现／接入；SVG 图表不是对标 ECharts 平滑曲线，交互、像素外观和原系统统计口径仍需进一步核实。不能把这一入口或整体项目标记完整复刻。

## 主体图表引擎对齐（2026-09-12）

- 从对标 RequireJS 映射查得核心库位置 `assets/js/echarts.min.js`，确认 ECharts 4.9.0；本地引入同版本上游发布版及许可证。walden 主题从对标主题文件仅提取 JSON 配置，不执行远端代码。
- 替换主体统计 SVG，按对标控制器配置平滑面积线、注册用户数图例、轴 tooltip、配色、线宽与网格。使用实际主体接口的 31 天日期和数量。
- 新增 ResizeObserver 调整尺寸、离开／刷新移除 DOM 时 dispose 实例和监听，避免重复进入累积 canvas。
- 25 个 JS 文件语法检查通过；实际浏览器验证 31 个数据点、smooth 配置、轴提示、图例关闭、刷新后单实例、调整窗口后宽度一致及离开页面后实例清理。本轮未修改后端。
- 图表引擎与配置已对齐，页面全部像素及主体统计口径未完整验收；四个主体子页面仍未实现。整体复刻目标保持未完成。

## 主体游戏列表入口（2026-09-12）

- 读取 `assets/js/backend/agents/game.js` 并保存为 `agent-games-controller.js`，确认主体参数、游戏类型、独立广告封禁状态与金币异常概率等字段；广告封禁状态不使用广告启用开关代替。
- 新增游戏可空 `game_ad_status`／`game_lottery_num`，缺失数据保持空白；新增迁移 `f912caf85e74`，独立库升级／回退通过。列表 API 支持主体＋名称字面包含＋类型＋封禁状态交集过滤及概率／日期排序。
- 主体仪表盘接入游戏列表和返回数据统计的导航；新列表保持主体范围，提供筛选、排序、分页、图标预览及上架状态修改接口入口。各主体筛选状态分别保存在内存中。
- 24 项后端测试通过，验证主体隔离、特殊字符名称过滤、类型／状态交集及未知状态为空；实际浏览器验证入口、主体查询参数和空结果；26 个 JS 文件语法检查通过。未对真实业务记录进行上架／下架写入测试。
- 列表仍缺数据查看、配置操作、游戏新增／编辑、选择和完整工具栏、日期与更多隐藏筛选字段、对标封禁图标，以及像素对齐；其他主体子页面仍未接入。整体目标保持未完成。

## 主体游戏筛选补齐（2026-09-12）

- 根据已保存 `agent-games-controller.js`，补入游戏 key、横竖屏、独立广告启用开关、创建时间范围；与已实现的主体、名称、类型、封禁状态一起作用于真实列表查询。
- API 对游戏 key 使用字面包含搜索，保留 `%` 字符；日期范围归一化到 UTC 后查询，支持边界相等，反向范围返回 422。广告启用状态和封禁状态仍为独立过滤条件。
- 前端创建时间接入日期选择器；无效输入保留表单与原值，并在表单下显示错误；重置清空新增筛选但保留当前主体范围。
- 24 项后端测试通过（扩充主体游戏用例），验证组合条件、北京时间边界和反向范围；浏览器验证实际请求参数、无效日期可修改、重置。26 个 JS 文件语法检查通过。
- 日期／隐藏字段筛选已有这一轮实现；搜索选择器样式、完整工具栏、收益与用户数据弹窗、配置操作、封禁图标、分页和视觉仍未完成，整体目标保持未完成。

## 游戏用户数据的登录日志数据源（2026-09-12）

- 读取并保存 `games/userdata.js` 为 `game-userdata-controller.js`，确认“用户数据”为多页签，包含抽奖、风控、提现、会员、登录日志、日活及图表；不能简单替换为会员列表。
- 登录日志页签字段确认：ID、会员 ID、用户账号、游戏名称、设备号、IP、登录时间。新增独立 `MemberLoginLog` 模型和 `/games/{game_id}/login-logs` 查询接口，支持游戏固定范围、会员／账号／IP／时间过滤、排序与分页。
- 没有历史来源时保持日志为空，不从 Member.last_login_time 生成记录。删除会员／游戏时检查日志依赖，避免生成孤儿日志。
- 25 项后端测试通过，覆盖游戏隔离、同会员多次登录、账号特殊字符、北京时间边界、分页、关联名称与删除保护。迁移 `a913db096f85` 独立升级／回退通过，本地运行服务 OpenAPI 确认新接口已加载。
- 本轮仅补齐该页签数据源，尚未接入完整用户数据弹窗；游戏登录事件采集／历史导入、其余页签模型及交互仍未完成。无前端变更，未重复浏览器测试。整体一比一复刻保持未完成。

## 后端源码：广告导入校验与错误提示修复（2026-09-12）

- 修复整数先转 float 导致 ID 精度丢失、非整数被截断的问题；使用 Decimal 校验整数与数据库范围。SQLite 按 64 位校验，其他数据库按 Integer 的 32 位范围校验。
- 拒绝 NaN、Infinity、溢出数值以及包含问号的非法金额，按行保存导入错误；同批合法记录仍可导入。带时区的观看时间在入库前转为 UTC，避免 SQLite 丢弃时区后时间偏移。
- CSV 启用严格格式解析，未闭合引号返回 422。恢复导入文件、数字／日期校验、公共必填校验、JSON 配置、主体循环层级、审核拒绝原因、提现不存在与生产启动配置的相关中文提示。中文 request_id 表头对应的错误记录现可保留请求 ID。
- 验证：27 项后端测试通过；新增用例覆盖非法数值与合法记录混合导入、超过浮点精度的 ID、时区归一化、错误记录持久化、空文件／非法编码／损坏 CSV。26 个 JavaScript 文件语法检查通过。
- 本轮测试使用临时 SQLite 数据库，没有修写现有业务数据或重放生产导入。非 SQLite 数据库未实测；尚未修复的历史损坏数据、其他页面功能和整体视觉对标不在本轮完成范围内，完整复刻仍未完成。

## 游戏用户数据：实际结构核对与抽奖数据源（2026-09-12）

- 新增 `inspect_reference_game_userdata.py`：使用 HTTP 会话只读获取页面及列表；解析 HTML 时关闭 JavaScript 并阻断网络，不执行对标依赖。记录页签名称、表格 ID、接口总数和字段名，不保存会员密码等原始记录值。
- `game-userdata-structure.json` 确认七个页签依次为抽奖列表（默认）、风控历史、提现记录、用户列表、登陆历史、每日活跃、数据统计。此次所查游戏实际接口返回抽奖 42 条、用户 7 条、登陆历史 39 条；风控、提现、日活为空。该结果仅代表所查游戏，不能推断全站为空。
- 新增独立 `LotteryRecord` 及 `/api/v1/games/{game_id}/lottery-records`：固定游戏范围，支持会员 ID、账号字面包含、类型、IP、内网、会员当前白名单、状态、时间区间、金币／时间排序和分页。返回控制器要求的金币、ECPM、风控标签及广告交易标识字段；未知内网／状态保留 null。未从广告流水伪造抽奖历史。
- 删除游戏／会员时增加抽奖记录依赖检查。迁移 `b913ec1a7096` 在临时 SQLite 升级、回退、再次升级及索引检查通过。本地运行服务 OpenAPI 已加载接口。
- 28 项后端测试通过，新增测试覆盖组合筛选、每个筛选项、跨游戏隔离、北京时间边界、未知值、排序分页、无效参数和删除保护。修正整数 Literal 查询参数拒绝 URL 字符串 0/1 的问题，改为带边界校验的整数参数。
- 本轮完成结构证据及抽奖查询数据源，尚未连接七页签弹窗、历史采集／导入或图表。没有将对标样本写入本地业务库；当前空表不代表对标没有记录。前端无变更，未宣称视觉或完整用户数据功能已完成。

## 游戏用户数据弹窗：抽奖与登陆历史（2026-09-12）

- `game-userdata.js` 新增 80%×80% 用户数据弹窗，默认抽奖列表，可切换登陆历史；入口位于主体游戏列表每行“用户数据”，对齐对标页签名称和基础字段。
- 两个已验证数据源支持字面账号搜索、IP／类型／内网／状态筛选、北京时间范围、排序、分页（10/15/20/25/50）、列显隐、卡片视图及 JSON/XML/CSV/TXT/DOC/XLS 导出。导出按全部筛选结果分页获取并检测总数变化、重复 ID 和空批次。
- 弹窗支持最大化、键盘左右切换页签、Escape 关闭菜单／日期选择器、关闭时取消请求和清理日期组件；刷新后页码超界自动收缩。表格文本统一转义，保留未知状态为空。
- `verify_game_userdata_browser.py` 已通过：真实登录和主体游戏入口、两个页签、分页排序、筛选与重置、日期范围及 UTC 参数、列显隐、卡片切换、205 条数据跨页六种格式导出、页码收缩、最大化、错误响应保留列表、日期组件清理和重开状态复位。截图 `visual-baseline/verified/game-userdata-dialog.png` 已保存。27 个 JS 文件语法检查通过，28 项后端测试通过。
- 风控、提现、用户列表、每日活跃、数据统计页签仍未接入；慢请求关闭后的陈旧响应专门测试脚本有调用问题，未将其标记为通过。整体一比一复刻继续未完成。

## 游戏用户数据弹窗：风控与用户列表接入（2026-09-12）

- 新增“风控历史”和“用户列表”页签，分别接入现有 `/risk/history` 与 `/members` 接口，并强制追加当前游戏 ID，避免跨游戏数据混入。
- 风控页显示会员、账号、游戏、IP、内网、标签和创建时间；用户页显示账号、昵称、收益金币、可用／冻结金币、内部号、达标、白名单、状态等对标字段。沿用弹窗已有筛选、排序、分页、列显隐、卡片和导出能力。
- 未将风控缺失的 `network_status` 或每日活跃历史从其他字段推导；未知值继续留空。27 个 JS 文件语法检查、28 项后端测试通过。
- 提现、每日活跃、数据统计及其专属后端历史模型仍未完成，整体目标保持未完成。

## 每日活跃独立数据源（2026-09-12）

- 新增 `DailyActivity` 模型及 `/games/{game_id}/daily-activity` 查询接口，字段为日期、日活人数和游戏名称，支持日期范围、排序、分页及固定游戏范围。
- 用户数据弹窗第六页签已从占位改为真实接口页签；日期筛选转换为 `date_from/date_to`，不会把日活从会员最后登录时间推导。
- 新增迁移 `d913fe3c92b8`，索引检查及现有 30 项后端测试、27 个 JS 文件检查通过。统计页签仍待独立接口和模型证据，完整复刻保持未完成。

## 数据统计页签接入（2026-09-12）

- 新增 `/games/{game_id}/statistics`，仅聚合当前游戏已持久化的广告收益和会员数量，返回总金额、总会员、今日新增、今日登陆等对标卡片字段；跨游戏数据不会混入。
- 弹窗第七页签显示统计卡片。由于当前系统没有可靠的历史事件时间线，今日新增／登陆保持 0，不从最后登录快照推导，避免产生虚假统计。
- 新增统计接口测试后，后端 31 项测试通过，27 个 JS 文件检查通过。统计图表、金额分时曲线和账号地域图仍缺少可验证独立来源，完整一比一复刻保持未完成。

## 统计趋势数据与可视化（2026-09-12）

- 统计接口新增当前游戏按日期聚合的收益序列和 `DailyActivity` 日活序列；前端统计页以横向条形图显示金额和日活，空序列显示暂无历史记录。
- 聚合严格限定 `game_id`，仅使用数据库已持久化记录；未把最后登录时间或广告记录推断成日活。统计卡片继续显示总金额、会员数和历史不可得的今日指标。
- 统计卡片的今日新增按北京时间当天的 `Member.created_at` 计算，今日登陆按真实 `last_login_time` 解析并统计；无效时间值跳过，不影响接口返回。
- 新增测试覆盖每日活跃／收益序列及游戏隔离；后端 31 项测试、27 个 JS 文件检查通过。对标中的 ECharts 复杂图表、地域分布和分时指标仍未完成，整体目标保持未完成。

## 七页签完整呈现（2026-09-12）

- 弹窗现在显示对标的七个页签：抽奖列表、风控历史、提现记录、用户列表、登陆历史、每日活跃、数据统计。前五个页签已接入可核实的本地查询数据源；后两页签显示数据源未验证提示，避免把会员快照或广告记录冒充日活／统计历史。
- 前五页签共享筛选、排序、分页、列显隐、卡片和导出能力，当前游戏范围固定传递。前端语法检查通过，后端 30 项测试通过。
- 后两页签仍需根据对标实际接口补建独立日活和统计模型，当前页面呈现属于诚实占位，不能视为一比一数据完成。完整复刻目标继续未完成。

## 用户与风控页签查询一致性修复（2026-09-12）

- 复核发现上一节“沿用筛选／排序能力”的结论过宽：用户接口此前忽略 `sort/order`、`is_true`、`game_addiction_enable` 和 `created_from/to`；风控接口忽略内网状态及排序。现补入这些实际查询条件，创建时间按 UTC 归一化并校验反向区间，排序采用白名单与 ID 次序保证分页稳定。
- 风控页标签从不存在的 `tag` 改为当前模型 `tags`，支持标签精确筛选。新增 nullable `RiskRecord.network_status`、开发库补列及迁移 `c913fd2b81a7`。历史未知状态保留 null，没有从 IP 推断内网状态。
- 验证：30 项后端测试通过；新增用户和风控测试覆盖游戏隔离、0 值筛选、未知状态、时间边界、金币／时间排序、分页和无效参数。迁移在隔离 SQLite 中升级／回退／再次升级通过。27 个 JS 文件语法检查通过。
- 浏览器验证增加风控与用户页签的真实本地接口请求、固定游戏 ID、内网／内部号、时间筛选与排序参数；四页签回归和原有导出／清理测试通过。真实本地查询验证与隔离库断言共同支持本轮结论，不能据此认定整体复刻完成。
- 仍缺风控去重工具、用户切换状态／编辑／金币操作等专属交互，以及提现、每日活跃、统计页签。完整视觉和历史数据接入仍待完成。

## 用户数据提现页签接入（2026-09-12）

- 新增提现记录页签，接入 `/withdrawals` 并固定 `game_id`，字段覆盖会员／账号／游戏、收件人、联系方式、金额、审核状态、拒绝原因及申请／更新时间；沿用统一筛选、排序、分页、列显隐、卡片和导出行为。
- 对标金额原始单位与现有接口保持一致，未在数据源层重复换算；状态 4 映射由后端保持兼容。每日活跃与统计仍没有可验证的独立历史接口，本轮没有使用登录快照或广告流水推导。
- 验证：30 项后端测试、27 个 JS 文件语法检查通过；此前游戏用户弹窗浏览器回归仍通过。提现页签新增前端接入尚未完成独立真实数据截图验证。

## ???????????2026-09-12?

- ?????????????????????????????????????????????????????????
- 27 ? JS ?????31 ??????????????? ECharts ?????????????????????

## ȫ����Ϸ����������ڣ�2026-09-12��

- ȫ����Ϸ�����б������û����ݺ��������ݰ�ť�������巶Χ��Ϸ�б�����һ�¡�
- ͨ�������ű������б���Ⱦ�������дԭ���б���ѯ��28 �� JS �ļ��﷨���ͨ����

## ȫվ�ع���ȫ����Ϸ�����֤��2026-09-12��

- ����ִ�� audit_pages.py���Ǳ��̡���Ա�����塢��Ϸ����桢���֡������������ˮ����ء��������Ϻͽ̳�ҳ��ȫ������ͨ������ҳ������ 4xx/5xx ��Ӧ��
- ��ʵ�������¼�����ȫ����Ϸ��������̬�б����ֵ������û��������������ݰ�ť��28 �� JS �ļ��﷨���ͨ����
- ҳ�����ͨ�����������ؼ���ɣ�ͳ��ͼ��ϸ�ڡ����ֱ༭��֧�����豸����Ȳ����Ա�����

## ??????????2026-09-12?

- ??????????????????? 237?statistics ? daily-activity ??? 200???????? 401??????????
- ????????????
