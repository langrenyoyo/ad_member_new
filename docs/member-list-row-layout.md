# 会员列表数据行布局

使用 `compare_members_fixture.py` 在两端注入相同三条虚构会员数据，重新采集列表。对标实测数据行高 47px，单元格字体 14px、行高 20px、上下内边距 8px、左右内边距 15px。本地此前上下内边距 12px、字体 13px，导致实际数据行高 49px。

已按参考值修正本地数据单元格，实际行高恢复 47px。列表差异像素占比从 5.0068% 降至 4.3234%，主列表工具栏选择和编辑回归通过。对比脚本增加三行高度相等的断言；具体样式和几何数据保存在 `visual-baseline/fixtures/members/layout.json`。

后续实测定位表头差异：本地排序按钮撑高单元格，原表头使用 10px 上下内边距、20px 行高和 600 字重；对标内部容器使用 8px 上下内边距、24px 行高和 700 字重。已修正并补齐表格外边框，排序按钮明确采用 24px 高度与顶部对齐。相同三条数据下，两端表头均为 41px、数据行均为 47px、整个表格均为 183px。差异像素占比进一步降至 3.7791%。脚本新增表头和整体高度断言，并记录表头所有子节点的样式用于复核。

进一步修正列宽：普通表头左右内边距为 10px，可排序表头为 15px；排序箭头采用独立定位，不再额外撑宽文本。相同三条数据、默认列下，17 列中 15 列宽度与对标完全一致。状态列仍为本地 61px / 对标 60.765625px，操作列仍为本地 397.46875px / 对标 256.984375px。差异像素占比降至 3.3920%。新增首行单元格内容和宽度证据，并对已对齐的 15 列宽度进行断言。此结论仅适用于本次固定数据和视口。

只读检查确认对标行操作顺序为“改绑关系”“单APP行为”“修改金币”和图标编辑；此前记录中的“成就关系”为错误描述。`inspect_member_rebind.py` 已确认改绑窗口包含当前 ID、上级 ID、归属 ID、源头 ID，以及确定/重置按钮。表单字段证据在 `visual-baseline/reference-verified/member-rebind.json`，不包含真实会员值，未尝试保存。本地已有 parent_id、ht_id、ht_top_id 数据字段，但还缺少对应改绑入口与表单；对标保存时的关联更新和业务校验尚未验证。

后续已补齐改绑入口和表单，将主列表操作调整为对标的四项。新的同数据截图中操作列两端均为 256.984375px，因此默认 17 列已有 16 列宽度相同；主列表差异像素占比为 3.0973%。改绑功能的验证与未验证业务规则见 `member-rebind-verification.md`。

后续按实测图标修正状态和白名单开关，默认 17 列宽度已全部一致；当前主列表差异像素占比为 3.0114%，见 `member-switch-verification.md`。仍存在工具栏入口及筛选控件等视觉差异，关系联动等业务规则尚未验证。完整图片包含大量空白，百分比不能用于表示全站完成率。其他列表状态的旧截图不代表本轮全部重测。
