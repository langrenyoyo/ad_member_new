# 会员显示列核验

2026-09-17：根据已下载的对标 `members-controller.js`，加入会员列选择菜单。默认 16 个数据/操作列保持原顺序，另有独立的行选择列。可切换头像、上级 Id/账号、代理商名称、昵称、达标状态/时间、兑换状态、最后登录设备/IP/时间等隐藏列；这些字段均来自现有会员模型或列表接口关联查询，不以昵称冒充账号，不补造登录值。

切换列直接修改表格可见性，不重新请求接口，不清除已勾选的会员。排序、翻页及刷新保留当前页面会话中的列配置；至少保留一个数据/操作列。离开会员页面移除菜单。管理员专用倍数列仍需权限体系证据，未加入菜单。

验证：

- `node --check public/app.js` 通过。
- `verify_member_columns.py` 通过：默认列、隐藏字段值、北京时间、行选择保留、零额外请求、排序/翻页保留设置、最少一列、退出清理。
- `verify_member_toolbar.py`、`verify_member_sorting.py` 通过。
- `compare_members_fixture.py` 通过默认表头顺序断言，重新生成本地/对标/差异截图。差异像素占比 11.1499%，平均 RGB 差异 5.13623；只证明三条固定数据的默认列表状态。

尚未完成：对标兑换列为可操作开关，当前新增隐藏列展示兑换值；达标时间排序、头像预览方式、列菜单精确样式仍需对照。分页大小、卡片、导出、操作表单和权限仍有差距。本项不能证明全站功能/数据/视觉一比一或可上线。

## 后续补齐：兑换与达标时间

上述兑换只读和达标时间不可排序的问题已修复。兑换开关通过 PATCH `/members/{id}` 保存 `exchange_enable`，保存期间禁用按钮，拒绝时保持原值并显示接口错误；成功后仅在原页面及原请求代次仍有效时刷新，防止切换主体/页面后刷新旧内容。前端不模拟成功，后端沿用现有字段权限校验。

会员列表服务端排序白名单加入 `game_addiction_time`，前端隐藏列启用排序按钮；日期字段沿用现有字符串存储与展示，不额外猜测时区。后端测试使用两个明确日期验证先排序后分页，覆盖升序首页及降序偏移。

`verify_member_exchange.py` 覆盖模拟接口的成功、403 拒绝、保存中重复点击、导航后旧响应返回、主体范围保留；`verify_member_sorting.py` 扩展至六个排序字段；`verify_member_columns.py` 通过。后端完整 41 项测试通过，其中已有测试验证兑换字段权限和落库。

这些证据不涵盖对标真实业务记录的修改；对标仍只读。隐藏列的像素级交互对比、非标准日期存储排序、头像预览和全站权限还需核验。默认三行截图未因本次隐藏列改动重拍，沿用上次默认状态证据。
