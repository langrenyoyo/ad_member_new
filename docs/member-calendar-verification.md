# 会员创建时间日历

对标运行时配置已保存到 `visual-baseline/reference-verified/member-date/options.json`：双日期选择、`timePicker=false`、自动应用、不自动填充，实际输出格式 `YYYY-MM-DD HH:mm:ss`；快捷项包括今天、昨天、最近 7/30 天、本月、上月。

本地复用已加载的日期组件，为会员 `create_time` 输入绑定日历；既有提现/主体页面默认绑定选择器不变。支持快捷范围、手输、Escape 关闭、重置。组件在输入随刷新或导航移除时销毁。空输入占位文字改为与对标一致的“创建时间”。

后端此前只能解析日期，新增解析日历输出的秒级北京时间范围，并转换为 UTC 存储时间查询；起止秒包含端点。原日期输入继续按北京时间自然日使用半开区间。混合日期/时间格式、倒序和溢出输入返回 422，不静默忽略。

验证：`verify_member_calendar.py` 在洛杉矶浏览器时区固定 UTC 时间，确认今天/昨天仍按北京时间生成；验证主体范围、提交、Escape、重置及组件清理。`verify_member_request_recovery.py` 通过。后端完整 42 项回归通过，包含秒级范围的两个端点、等时范围、相邻日期及无效输入。

本次默认列表截图重新生成，但未完成打开日历后的双端像素对比。共享组件的月份/星期标签与对标配置仍有差异，日历不能宣称已完全视觉一比一。真实部署服务是否载入本次后端改动尚未单独验收。

## 展开日历对照补充

新增 `compare_member_calendar_fixture.py`，使用相同三行记录及明确日期 `2026-09-10 00:00:00 - 2026-09-16 23:59:59` 在两端展开日历，独立生成截图、差异图、数据和配置，加入汇总报告。

原本地日历为 Arial、12px 日期文字、18px 行高，日历总尺寸 662×296；对标为 Source Sans Pro 字体栈、13px 日期文字、约 20.43px 行高，总尺寸 660×313.375。已用会员页面作用域样式修正字体、行高和外框，并提供会员专用 locale 覆盖，将月份改为数字月份、“确定”改为“应用”。未修改其他页面默认 locale。

星期标签经读取运行时配置确认本来一致，无须再次调整：插件会根据 firstDay 旋转数组，直接复制已旋转的数组反而会错位。对照脚本新增完整运行时 locale 相等断言，并验证两端起止时间相同。`verify_member_calendar.py` 再次通过。仍未证明所有月份、日期选择过程及窄屏布局完全一致。
