# 主体统计与游戏列表复验

- 修复主体游戏列表 API 地址 `/games===` 为 `/games?`，恢复主体范围查询；删除重复游戏列表入口，修复空结果列跨度。
- 主体图表此前为四组收益/点击曲线，与对标控制器不同。现恢复单组注册用户平滑面积图、配色、图例与坐标配置。
- `verify_agent_games_fixture.py` 用 25 条浏览器模拟记录验证主体范围、分页、排序回首页、禁用状态筛选、空结果和重置，不写业务数据库。
- `verify_agents_browser.py` 在本地 3000 端口通过完整主体流程；日期测试先用 Escape 收起日历，再点击提交，保留无效时间校验。`verify_chart_lifecycle.py` 通过渲染、尺寸更新和卸载清理。
- 新增 `compare_agent_dashboard_fixture.py`：对标与本地注入相同主体名称、31 天注册数据及今日数量。修复多余标题栏、卡片图标、导航按钮及图表面板位置后，内容区差异像素占比从 37.9033% 降至 0.3280%，平均 RGB 差异 0.30631。截图、数据和原始指标已加入汇总报告。
- 本轮不证明整个主体模块一比一。统计按钮和文字仍有视觉差异；数据分析、主体用户/提现入口及游戏配置仍需对标和范围验证。
