
# 主体列表布局核验

使用 `compare_agents_fixture.py` 在对标与本地注入相同三条主体记录，视口和截图裁剪保持一致。修正主体列表表格边框、Source Sans Pro 字体、表头 14px/24px/700、表头 41px，以及数据行 14px/20px/47px/左右 10px 内边距。

最新几何证据：两端表格高度 183px，首个数据行高度 47px；工具栏相对内容坐标均为 52.5625px。差异像素占比由 2.7297% 降至 2.0859%。表格和行几何断言保存在 `visual-baseline/fixtures/agents/layout.json`，截图、差异图和数据在同目录。

该结果只覆盖三条固定主体数据和默认列表状态。主体工具栏、权限可见性、下级进入入口、筛选选择器和后端统计口径仍需独立验收，不能将此局部视觉结果当作全站一比一或上线证明。
