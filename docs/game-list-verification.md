# 游戏列表当前核验

`verify_agent_games_fixture.py` 使用浏览器隔离响应验证游戏列表的主体范围、25 条数据分页、排序后回到第一页、禁用主体筛选和空结果状态，未写入业务数据库。该脚本通过。

`verify_game_form_browser.py`：全局/主体范围新建与编辑、数值及 JSON 校验、范围约束、移动端窗口和写入请求通过。

主体列表本轮已完成固定三条数据的双端几何修正：表格 183px、表头 41px、数据行 47px。游戏新增/编辑窗口现已覆盖基础信息、运行配置、分佣提现、微信及高级 JSON 配置，支持全局和主体范围，保存校验、权限入口、移动端几何和迟到请求隔离已有浏览器回归；删除仍沿用关联数据保护。完整工具栏、逐像素双端列表截图和全部权限场景仍待验收。
