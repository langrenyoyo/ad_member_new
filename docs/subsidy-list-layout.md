# 补贴列表对比记录

## 已生成的数据

`compare_subsidies_fixture.py` 会生成三条固定补贴记录，并同时写入本地接口形状和对标接口形状：

- `visual-baseline/fixtures/subsidies/data.json`
- 状态覆盖：申请中、审核通过、审核失败
- 金额覆盖：小数、普通金额、边界金额
- 图片覆盖：多图、空图
- 文本覆盖：中文及 `<>&` 特殊字符

脚本默认只生成数据；设置 `SUBSIDY_COMPARE_CAPTURE=1` 后采集真实对标页面与本地页面。仅允许对标登录 POST，业务写请求被拦截；三条数据只替换浏览器响应。图片使用同样的固定 SVG 响应，不依赖外部图片服务器。

## 当前结论

已修正对标入口拼写（`DmvTqXBpfF.php`），成功完成实时双端采集。之前所谓登录/网络阻塞不能据旧脚本认定。失败时报告保持无效并返回错误，禁止再用旧空表截图代替当前三条数据的对标截图。

当前报告 `comparison_valid=true` 仅表示采集条件有效，不表示页面已经一比一。视口内容区 1690×1030、两端筛选折叠、相同记录 ID、图片加载完成、列顺序一致、表格 183px 和全部数据行 47px 均已断言通过。

修正行为列位置（到账金额后）、VIP/审核状态徽章、工具栏留白及图标、字体与行高、30px 缩略图。有效同数据内容区差异像素占比从 7.6549% 降至 3.9369%，平均 RGB 差异从 5.20347 降至 2.99702。差异占比分母包含空白区域，不能作为完成率。

补贴页已补齐对标入口对应的工具栏编辑/删除、行内编辑/删除，以及批量同意/拒绝。编辑器会重新读取详情，只提交发生变化的字段；金额、拒绝原因、失败重试、重复提交锁定、路由切换隔离均已覆盖浏览器验证。删除使用单条或批量接口，批量删除缺失 ID 时原子失败并写入管理员审计。

由于对标编辑表单字段无法在不提交真实业务写操作的前提下读取，编辑器字段级内容暂按列表返回字段和本地接口契约实现，不能宣称字段级像素一比一。补贴图片回归、CRUD 浏览器验证、后端接口测试与 JS 语法检查通过；单元格搜索入口和部分列宽/间距仍存在视觉差异。

## 单元格筛选

新增会员 ID、账号、收件人、联系方式四个点击筛选入口。采集脚本在截图后分别点击真实对标和本地相应单元格，保存请求参数至 `visual-baseline/fixtures/subsidies/searches.json`，并断言相同字段值与对标运算符（账号 LIKE，其余等值）。本地已存在的后台条件使用账号子串匹配与其他三字段等值查询。本次浏览器响应由固定数据替换，验证请求契约，不代表对标后台真实匹配结果已认证。

本轮采集和 JS 检查通过，内容区差异像素占比为 3.9234%。游戏名称及隐藏列的搜索入口、完整编辑/删除/审核交互仍未完成。
