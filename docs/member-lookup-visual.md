# 游戏筛选下拉层：同数据对比

`compare_member_lookup.py` 在两端选项接口返回相同 12 条虚构游戏记录，采集首屏 10 条、键盘右方向键翻页后的末页 2 条，以及无匹配搜索结果。对标登录后只允许读取请求，选项 POST 在浏览器内直接返回生成数据，其他业务写入被拦截。

发现本地下拉层附加到 body 后继承 Arial/21px，而对标为 Source Sans Pro/22.001px。列表项继承后的行高分别为 19.5px 和 20.4295px，使首屏高度相差 9.21875px。已为会员下拉层添加专属类和字体行高，不更改其他控件。

修正后三个状态两端 DOM 宽高、文本均相同：宽 300px，首屏高 336.21875px、末页高 92.84375px、空结果高 47px。脚本新增宽高及文本相等断言。原始截图、样式和数据分别保存在 `visual-baseline/fixtures/member-lookup-first`、`member-lookup-last`、`member-lookup-empty`。

截图仍有差异：本地下拉层相对内容 x 坐标与对标相差 0.328125px，浏览器裁剪后的图像宽度因此为 300px / 301px；报告按白色补齐画布比较，不缩放图像。当前首屏差异像素约 5.62%、末页 5.45%、空结果 3.48%。不是像素完全一致，也不验证真实账号权限、选项来源范围或后端 ID 筛选语义。代理商控件尚未单独采集这些展开状态。

## 代理商展开状态补验

通过 `PARITY_LOOKUP_KIND=agent` 运行同一脚本，使用 ID 6200–6211 的 12 个虚构代理商。首屏 10 项、末页 2 项、空结果 0 项均逐项校验 ID；同时断言两端文本、宽高一致，扣除侧栏 230px 和顶栏 50px 后弹层 x/y 也相同。页间切换使用真实键盘右方向键，不直接调用插件翻页函数。

最初截图边缘混入下方不同会员数据的像素，现两端会员列表都改为空数据夹具；对标选项请求仍全部在浏览器内返回虚构选项，未进行业务写入。代理商最新差异像素占比为首屏 0.0187%、末页 0.1107%、空结果 0%。原始图片包含边框抗锯齿和底层页面背景，不屏蔽这些像素。证据在 `member-lookup-agent-first/last/empty` 三个目录，已加入统一报告。

这些证据补齐代理商展开状态，不证明真实主体可见范围与服务端筛选语义。前述游戏控件截图仍为其上次采集结果，未因代理商通过而宣称游戏所有像素一致。
