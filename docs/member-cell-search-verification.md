# 会员单元格搜索

根据 `visual-baseline/reference-verified/members-controller.js` 的列 formatter 和 `table-controller.js` 的 `formatter.search`，账号、上级 Id、游戏名称、代理商名称、昵称、注册 IP 改为可点击搜索。隐藏列需先通过显示列菜单打开。游戏名称、代理商名称和昵称按对标列定义左对齐。

点击按列值写入相应筛选并回到第一页，保留已应用的其他条件、排序、每页数量及路由主体范围；筛选面板仍保持原展开/折叠状态。使用转义后的文本和属性，不将数据解释为 HTML。空值不生成入口，数字 0 正常生成入口。

`verify_member_cell_search.py` 验证六个字段、含引号和 HTML 字符的账号、上级 Id 为 0、跳回第一页、每页 20 条、排序和 VIP 条件、主体范围、面板折叠及重置。`verify_member_columns.py` 和六字段排序回归通过。`node --check public/app.js` 通过。

重新运行 `compare_members_fixture.py`，默认三条数据表头断言通过；差异像素占比 6.6367%，平均 RGB 差异 4.67041。此结果只覆盖默认列表截图。对标 `last_login_ip` 列也声明了搜索 formatter，但同时 `operate:false`，其实际过滤行为还需独立核对，本次没有猜测实现。后端现有文本筛选规则是否与对标每种点击搜索运算符完全一致，仍需双端数据证据。

## 后续实际请求核对

`inspect_member_search_contract.py` 用浏览器模拟行逐项点击对标列，拦截列表请求并以模拟行响应，保存 `visual-baseline/reference-verified/member-search-contract.json`。没有写入对标业务记录。实际运算符：账号/代理商名称/昵称为 `LIKE`，上级 Id/游戏名称/注册 IP 为 `=`；逐次点击保留先前已应用条件。最后登录 IP 在给定非空值 `198.51.100.25` 时点击也不发出列表请求，未据此添加虚构的后端过滤功能。

发现并修复本地游戏名称、注册 IP 原先均采用包含匹配的问题，改为精确匹配。新增后端测试使用 `exact-filter-game` 与其 `-extra` 后缀、`192.0.2.1` 与 `192.0.2.10` 验证不会混入相似记录，且账号仍允许包含匹配。组合条件及分页结果验证通过，完整后端 42 项测试通过。LIKE 对百分号/下划线等特殊输入在对标后端的解释仍需进一步证据，本次未改动已有转义规则。

## VIP 徽标搜索补齐

对标卡片 DOM 的 VIP 徽标亦使用 `searchit`。扩展实际请求捕获脚本后确认：点击 V0 发送 `vip=0`，运算符为 `=`，其他已应用条件保留。对应请求已更新在 `member-search-contract.json`。

本地 VIP 徽标已改为可点击搜索，沿用现有后端 VIP 精确匹配；筛选下拉按对标列 `searchList` 限定为 V0/V1/V2，移除无依据的 V3–V10。后端未因此删除或更改任何会员等级数据。缺失 VIP 值留空，避免渲染 `Vundefined`。

浏览器测试验证下拉选项、卡片内点击徽标子元素、V0 保留、回到第一页、主体和每页条数保持，以及重置移除 VIP 条件。会员卡片回归通过；默认列表与卡片截图均重新生成。此项未改变其余权限及操作流程尚待核验的结论。
